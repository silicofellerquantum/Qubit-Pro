global:
  scrape_interval: 5s     # Scrape targets every 5 seconds (high frequency for real-time simulation updates)
  evaluation_interval: 5s # Evaluate rules every 5 seconds

scrape_configs:
  - job_name: 'quantum_studio_backend'
    static_configs:
      - targets: ['backend:5000']
